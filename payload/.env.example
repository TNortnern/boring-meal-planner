# ===============================================
# BORING Meal Planner - Payload CMS Environment
# ===============================================

# Database (Railway PostgreSQL)
# Railway will auto-inject DATABASE_URL - use this mapping:
DATABASE_URI=${{DATABASE_URL}}

# Payload CMS Configuration
PAYLOAD_SECRET=your-super-secret-key-min-32-chars
PAYLOAD_PUBLIC_SERVER_URL=https://your-payload-app.railway.app

# Server Port (Railway sets PORT automatically)
PORT=3000

# Bunny CDN (Optional - for image uploads)
BUNNY_API_KEY=your-bunny-api-key
BUNNY_STORAGE_ZONE=your-storage-zone
BUNNY_CDN_URL=https://your-storage.b-cdn.net
BUNNY_CDN_HOSTNAME=your-storage.b-cdn.net

# ===============================================
# RAILWAY DEPLOYMENT NOTES:
# ===============================================
# 1. Add PostgreSQL service from Railway dashboard
# 2. Railway auto-injects: DATABASE_URL, PORT
# 3. Required variables to set manually:
#    - PAYLOAD_SECRET (generate with: openssl rand -base64 32)
#    - PAYLOAD_PUBLIC_SERVER_URL (your Railway app URL)
# 4. Optional (for image uploads):
#    - BUNNY_API_KEY, BUNNY_STORAGE_ZONE, BUNNY_CDN_URL
